const params=new URLSearchParams(location.search);const product=params.get('product')||'Keepsake';const title=document.querySelector('#productTitle');const form=document.querySelector('#personalizationForm');const previewTitle=document.querySelector('#previewTitle');const previewMessage=document.querySelector('#previewMessage');const status=document.querySelector('#personalizationStatus');if(title)title.textContent=product;form?.addEventListener('input',()=>{previewTitle.textContent=form.elements.title.value||'Your Family Story';previewMessage.textContent=form.elements.message.value||'A dedication will appear here.'});form?.addEventListener('submit',(event)=>{event.preventDefault();localStorage.setItem('friendlyGenealogyPersonalization',JSON.stringify({product,...Object.fromEntries(new FormData(form))}));status.textContent='Your personalization draft is saved in this browser. No order has been placed.';});